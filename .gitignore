# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
.vite/
*.log
*.tsbuildinfo

# Editor
.vscode/
.idea/
.DS_Store

# Local secrets (never commit)
.env
.env.local
.env.*.local
service-account.json
*.service-account.json
google-drive-credentials.json

# Aegis runtime
backend/aegis.db
backend/aegis.db-*
backend/aegis_test.db
backend/aegis_test.db-*
aegis_test_data_*/
backend/aegis_test_data_*/
pytest-of-root/
backend/pytest-of-root/
backend/data/aegis.db*
backend/data/uploads/
# Verified page batches from the GPT PDF-to-ACSD reader, keyed by content.
backend/data/pdf-acsd-cache/
# Durable content-addressed store for published image crops (Q8 step 10).
backend/data/source-asset-store/
# The append-only generation output is runtime state, not source.
backend/data/bulk_import_output.xlsx
*.aegis-checkpoint.json
*.xlsx~

# Create Workbooks runtime state (generated PDFs + vendored tool caches)
backend/data/workbooks/
backend/aegis_pipeline/create_workbooks/output/
backend/aegis_pipeline/create_workbooks/mmd_cache/
backend/aegis_pipeline/create_workbooks/plan_cache/

# data/bulk_import_database.xlsx IS committed — it is the database fixture.
# Syllabus structure workbooks under backend/data/syllabus/*.xlsx SHOULD be
# committed so units/chapters preload on deploy (not runtime-generated).

# Runtime caches of hash-named model output (derived state, never source).
# The committed chapter-reading fixture from #203 stays tracked: ignore rules
# never apply to already-tracked files.
backend/data/chapter_reading/
backend/data/question_polishing/

# Chapter-authoring script scratch output (generated by build_bulk_import/emit_canonical)
backend/scripts/chapter_authoring/out/
