# Aegis owner-order handoff — 2026-08-21

Written for the incoming implementer taking over ProjectAegis
(mohithmohan07/ProjectAegis). It compiles every order the owner gave in
the outgoing session's working period, what was delivered against each,
and what is still open. The repository's own record goes deeper:
`CLAUDE.md` (the binding doctrine), `docs/residue-ledger.md` (every
round's dispositions and open residues), `docs/aegis-restructure.md`
(the architecture), `docs/mobile-stores.md` (the store-apps runbook).

Repository state at handoff: `main` at `16e36f9`, all work pushed,
backend suite 2,738 passed / 7 xfailed, frontend 75 passed, builds
clean. **The deployment is likely BEHIND main** — see "Deployment
state" below.

---

## 1. Standing directives (binding, owner-issued, still in force)

1. **Respond as a report.** "You should always do as per the prompt I
   give you. Nothing more nothing less. No initiatives, unless told."
2. **Push directly to `main`** (`git push origin <branch>:main`). No
   pull requests unless explicitly asked.
3. **CLAUDE.md Rule 1 is not negotiable**: no deterministic judgment
   about meaning — no regexes/keyword lists classifying content, no
   numeric thresholds deciding meaning, no volume-derived structure.
   Meaning = model verdicts; mechanics = code. Mid-run defects route to
   The Fixer (Q13); critic dissent is advisory, never a gate (Q10);
   nothing is lost silently; finished work always ships.
4. **Keep `docs/residue-ledger.md` current** — every round appends its
   dispositions and residues.
5. **Owner deploys; implementer pulls/merges.** ("No you do the pull
   and merge. I will deploy.")
6. No model identifiers in commit messages, code comments, or any
   pushed artifact.

## 2. Orders given this period, in sequence, with delivery

| # | Owner's order (condensed, their words where short) | Delivered | Commit |
|---|---|---|---|
| 1 | "Can't assessments-related items in concept mapping run in parallel to reduce the time? Inventory extraction, creation of types and cases, polishing the questions" → "Okay lets go then" | Performance round 2: Place ∥ Analyse ∥ Polish as parallel lanes, prerequisite-capture riders on side threads, Phase 2 source-chunk fan-outs. Outputs byte-identical to sequential; knobs `AEGIS_PHASE3_DECISION_WORKERS`, `AEGIS_SOURCE_CHUNK_WORKERS` | `fc8f389` |
| 2 | "The assessments generation for source files of concept mapping is not dependent on any blueprint for pre or post… Master files has to be generated, there is a blocker." | Blueprint dependency removed from the generated lane (per-question model cell verdicts); materialize prompt names its exact answer fields (`assessment-materialize-3`); one impossible question blocks itself and is recorded (`materialization_blocked`, Ready-with-flags) instead of killing the whole Master | `fdac400` |
| 3 | "I don't want any human review band… rectified by the refiner/fixer. Review page with Topics, Concepts and its respective elements only (NO BLKs, critics), edit what's required and push to regenerate. Review re-entry shows the latest changes. Clear upload option for the downloaded/edited Excel to the CMS; remove upload-to-database. Post Master = everything incl. chapter, topics, concepts, groups, assessments (source only). Pre Master = same columns + around 5 Basic and 5 Intermediate generated questions per concept." | Human-review band retired (0.92 verdict-floor mechanics kept); review page filtered to editable elements; multiple review rounds re-enter on latest; edited-Excel upload applies as one recorded review round **and publishes to the CMS in the same act**, replacing the upload-to-database buttons; Pre coverage target recorded as ~10 (≈5 Basic + 5 Intermediate, never padded) | `c63f7d1` |
| 4 | "Why is that Post Learning block with so much space…? Keep the design clean… And the log block is black even with light mode." | Output card redesigned (four numbered outputs, folded reasons); console gets per-theme tokens, matches light theme | `c63f7d1` / `05148e4` |
| 5 | "Build the UI/UX such that… it should dynamically sit in my phone to use, conveniently." | Full responsive pass, audit-driven, visually verified at phone size (390×844, both themes) | `05148e4` |
| 6 | "No you do the pull and merge. I will deploy." | Four outstanding branches merged (re-issued syllabus workbooks + refresh-on-deploy with authored-content guards, MSBSHSE Grade 6 assessment assets, Fly staging config). The other ~140 stale branches judged superseded by the restructure — deliberately not merged | `2eb0833` (+3 merge commits) |
| 7 | "Why does the network keep getting lost when I switch tab?" → "It should be running in the background… When I go back it automatically works, keeping the previous status till then. No need to show 'reattaching to network…'" | Runs are genuinely background: durable per-job event journal (`run-events.ndjson`), silent journal catch-up on tab return (no banner), visibility-aware polling, `fly.toml min_machines_running = 1` so the machine no longer stops mid-run | `ee03b10`, `351d203` |
| 8 | "See how I am restricted to the logs when viewing on my phone. It should be flexible." | Phone console: bottom sheet with sticky header, expand-to-full-screen, model-usage folded | `eda53f7` |
| 9 | "Also this error came when I switched the tab [raw StreamTransportError]. This should be taken care as well." | Convert streams got the same reattach wiring as generate; transport errors read as sentences, never class names | `373d13f` |
| 10 | "I want both for App Store and Play Store, it can be universally used by anyone." | Three layers: installable PWA (manifest, narrow service worker, icon set — anyone can install from the browser today); native Google sign-in for store shells (`/auth/native/*` system-browser flow, 90-second single-use ticket over `aegis://`, 404 until `AEGIS_GOOGLE_CLIENT_SECRET` set); Play TWA (Bubblewrap, `mobile/twa/`) + iOS Capacitor (`mobile/capacitor/`) scaffolds, `/.well-known/assetlinks.json`. Full runbook: `docs/mobile-stores.md` | `1c2e8c3` |
| 11 | "Pre learning both files are coming out as an empty file" (+ job-64 diagnostics zip) | Root cause found from the zip: **poem chapter** — the Architect's `language_topology_plan` slot legitimately routes `task_qids`; carried verbatim into the Pre-lane rules it tripped the no-extraction guard, which refused the ENTIRE Pre map, so both Pre files projected empty. Fixed: source-question ids are redacted at the mint at all three Pre-lane sites (the guard's own doctrine for evidence the lane is *shown*); expository chapters unaffected, keys unchanged. Also: an empty Pre pair now states its recorded reason on the outputs card beside the still-live download (Rule E kept) | `fee1dbc`, `16e36f9` |

## 3. Deployment state — the single most important open item

The deployed Fly app (`projectaegis`, https://projectaegis.fly.dev) was
last known deployed at a build ~12 rounds behind main. Several symptoms
the owner reported live came from that stale build. The outgoing
session could not verify current deploy state (sandbox egress blocks
fly.dev).

Owner runs, from the repo root:

```bash
git pull
fly deploy
```

Quick fingerprint: https://projectaegis.fly.dev/manifest.webmanifest
returns JSON (`"name": "Aegis — Content Intelligence Engine"`) on a
current build, the app's HTML page on a stale one.

**After deploy: re-run generation on the job-64 poem chapter.** The
finished Post work replays from saved decisions at near-zero cost; the
Pre lane will now build (its 10 captured prerequisites become the Pre
concepts and their generated questions), and both Pre Master files
fill.

## 4. Owner-side to-dos (nobody else can do these)

- Deploy (above).
- Native sign-in: set `AEGIS_GOOGLE_CLIENT_SECRET` (Fly secret) and add
  `https://projectaegis.fly.dev/auth/native/callback` to the Google
  OAuth client's authorized redirect URIs.
- Store listings: Play Console account ($25 one-time) + Apple Developer
  ($99/yr, needs a Mac with Xcode); Android signing key →
  `AEGIS_ANDROID_CERT_SHA256`. Step-by-step: `docs/mobile-stores.md`.
  (The PWA install works for everyone before any store exists.)

## 5. Open residues (full detail in docs/residue-ledger.md)

- Assessments SESSION generate and Create Workbooks streams are not
  journaled — a tab switch there shows a readable transport message
  rather than silent catch-up.
- Live mid-run stream re-attach endpoint unbuilt (journal covers it in
  practice).
- Master-workbook edited-Excel import unbuilt (the Excel→CMS act covers
  the CONCEPT workbook; Masters re-publish from the release page).
- One-click regenerate-Masters affordance on the review page unbuilt.
- Blocked-question counter has no frontend surface (payload + logs +
  Ready-with-flags badge only).
- Deferred performance items: per-call OpenAI client reuse, phase36
  double-prepare, kernel deferred-critic primitive.
- `sw.js` has no test net (deliberately narrow scope); store
  screenshots/feature graphics not produced.
- Native-auth ticket single-use guard is per-process (fine at
  `min_machines_running = 1`; move to DB if scaling out).

## 6. Working conventions (how this repo is driven)

- Backend suite: `cd backend && S=$(mktemp -d) && mkdir -p $S/data &&
  AEGIS_DB_URL="sqlite:////$S/x.db" AEGIS_DATA_DIR="$S/data" python3 -m
  pytest tests/ -q -p no:cacheprovider`
- Frontend: `cd frontend && npx vitest run && npx tsc --noEmit && npm
  run build`
- Green suites before every push; push straight to `main`; append the
  round's dispositions to `docs/residue-ledger.md`.
- The four outputs are numbered by owner ruling OD4: 01 Pre Concept
  File, 02 Pre Master, 03 Post Concept File, 04 Post Master. Both
  manifest implementations (eager + lazy twin) must be edited together
  — `test_pre_release_lane_wiring.py` pins them.
- Rule E: a defect blocks the database write, never a download.
- When something ships empty or blocked, the reason must be visible
  where the owner is looking — "nothing silent" is the house rule the
  owner enforces most.
