t.compiler_version}
           {manifest.phase ? ` · ${manifest.phase.replace(/-/g, " ")}` : ""}
+          {semanticGraph?.version ? ` · semantic graph ${semanticGraph.version}` : ""}
         </div>
       </div>
       <div className="row">
diff --git a/frontend/src/types.ts b/frontend/src/types.ts
index 6bfbb5f..7292086 100644
--- a/frontend/src/types.ts
+++ b/frontend/src/types.ts
@@ -166,6 +166,16 @@ export interface SourceArtifactManifest {
     verified_repairs?: number;
     remaining_issues?: number;
   };
+  semantic_graph?: {
+    version?: string;
+    compiler_version?: string;
+    status?: "pending" | "passed" | "failed" | "unavailable" | string;
+    api_first?: boolean;
+    topics?: number;
+    subtopics?: number;
+    tasks?: number;
+    source_contract_sha256?: string;
+  };
   source_reconstruction?: {
     version?: string;
     compiler?: string;
